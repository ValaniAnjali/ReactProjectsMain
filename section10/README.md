Here We are seeing different option to get rid of props drilling.

1) Component Composition

We can use usecontext to share values and state updating function

if we dont want to use useContext hook than we can also use (Context component).Consumer