import React from 'react'

const ThemeButton = ({changeThemeMenu}) => {
  return (
    <div>
        ThemeButton
        <button onClick={changeThemeMenu}>Change Theme</button>
        </div>
  )
}

export default ThemeButton